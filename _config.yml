defaults:
  - scope:
      path: ""
    values:
      layout: "default"
      lang: en
      base-url: "/"
  - scope:
      path: de
    values:
      lang: de
      base-url: "/de/"

plugins:
  #- jekyll-sitemap
  #- jekyll-seo-tag
  - jemoji

sass:
  style: compact
